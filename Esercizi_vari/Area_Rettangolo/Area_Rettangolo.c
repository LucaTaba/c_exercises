#include <stdio.h>

int main() {
    int base;
    int altezza;

    printf("Inserisci la base: ");
    scanf("%d", &base);

    printf("Inserisci la altezza: ");
    scanf("%d", &altezza);

    printf("Inserisci un numero intero: %d ", base*altezza);

  return 0;
}